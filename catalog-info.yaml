apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: minicloud-backstage
  description: minicloud internal developer portal — org-owned Backstage image with Authentik SSO, Kubernetes, ArgoCD and TechDocs plugins
  annotations:
    github.com/project-slug: andrelair-platform/minicloud-backstage
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: production
  owner: user:default/andrelair
  system: minicloud-platform
  consumesApis:
    - minicloud-plane-api
