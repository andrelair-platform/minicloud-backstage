apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${{ values.name }}
spec:
  template:
    spec:
      containers:
        - name: ${{ values.name }}
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
