apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${{ values.name }}-tls
spec:
  secretName: ${{ values.name }}-tls
  issuerRef:
    name: minicloud-ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - ${{ values.name }}.devandre.sbs
    - ${{ values.name }}.10.0.0.200.nip.io
