apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${{ values.name }}
spec:
  template:
    spec:
      containers:
        - name: ${{ values.name }}
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
