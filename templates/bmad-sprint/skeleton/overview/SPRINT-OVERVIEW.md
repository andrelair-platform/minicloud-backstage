# Sprint: ${{ values.milestone_title }}

| Field | Value |
|-------|-------|
| **Project** | `${{ values.project_code }}` |
| **Milestone slug** | `${{ values.milestone_slug }}` |
| **GitHub Milestone** | ${{ values.milestone_title }} |
| **Tech stack** | ${{ values.tech_stack }} |
| **Backlog** | [${{ values.backlog_repo }}](https://github.com/${{ values.backlog_repo }}/milestones) |
| **Board** | [Project #${{ values.project_number }}](https://github.com/orgs/andrelair-platform/projects/${{ values.project_number }}) |
| **BMAD stories** | `bmad/stories/${{ values.project_code }}/${{ values.milestone_slug }}/` |

## Description

${{ values.sprint_description if values.sprint_description else "_Fill in sprint goals here._" }}

## Story index

| File | BMAD ID | Status |
|------|---------|--------|
| _(populated by stories in this directory)_ | | |

## How this sprint was created

Generated by the Backstage **New BMAD Sprint** scaffolder template.
Stories are pushed to GitHub Issues automatically when this directory is committed to `main`
via `.github/workflows/bmad-sync.yml`.

To add more stories:
1. Create `SNNN-slug.md` in this directory (copy from `bmad/templates/story-template.md`)
2. Commit + push to `main` — the sync action handles the rest
